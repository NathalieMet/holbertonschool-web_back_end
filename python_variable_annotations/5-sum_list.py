#!/usr/bin/env python3
"""def sum_list"""


def sum_list(input_list: [float]) -> float:
    """takes a float n as argument and returns the string representation of
    the float."""
    return sum(input_list)
